<% sub ||= "" %>
<% term ||= "(expand)" %>
<% style ||= "" %>

<div class="cl" style="text-align:center">
<div class="collapse-group">
	<<%=tag%> class="squeeze bold" style=<%="#{style}"%>> 
		<%=title%> 
		<% if sub != "" %> 
			<small><%= sub %></small>
		<% end %>
		<a class="bundle unbold" href='#'><%= term %></a> 
	</<%=tag%>>
<div class="collapse">